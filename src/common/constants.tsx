const SYMBOLS = {
  X: 'x',
  TILDE: '~',
  OPEN_PAREN: '(',
  CLOSE_PAREN: ')',
};

const UNIT = {
  ML: 'ml',
  PERCENT: '%',
};

const Constants = {
  SYMBOLS,
  UNIT,
};

export default Constants;